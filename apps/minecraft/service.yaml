apiVersion: v1
kind: Service
metadata:
  name: minecraft
  namespace: minecraft
  labels:
    app: minecraft
    app.kubernetes.io/name: minecraft
    app.kubernetes.io/part-of: acer-lab
spec:
  type: NodePort
  selector:
    app: minecraft
  ports:
    - name: minecraft
      port: 25565
      targetPort: minecraft
      nodePort: 30565
      protocol: TCP
