apiVersion: v1
kind: Service
metadata:
  name: nginx-test
  namespace: homelab-test
  labels:
    app: nginx-test
    app.kubernetes.io/name: sample-app
    app.kubernetes.io/part-of: acer-lab
spec:
  type: NodePort
  selector:
    app: nginx-test
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30080
